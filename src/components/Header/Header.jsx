import { Auth } from "../Auth/Auth";

const Header = () => {
   return (
      <header id="header">
         <h3>IK 230</h3>
         <div>
            <Auth />
         </div>
      </header>
   );
};

export default Header;
